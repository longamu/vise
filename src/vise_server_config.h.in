#ifndef _VISE_SERVER_CONFIG_H_
#define _VISE_SERVER_CONFIG_H_

#define VISE_SERVER_NAME "${VISE_SERVER_NAME}"

#define VISE_SERVER_VERSION_MAJOR "${VISE_SERVER_VERSION_MAJOR}"
#define VISE_SERVER_VERSION_MINOR "${VISE_SERVER_VERSION_MINOR}"
#define VISE_SERVER_VERSION_PATCH "${VISE_SERVER_VERSION_PATCH}"
#define VISE_SERVER_URL "${VISE_SERVER_URL}"

#define VISE_SERVER_AUTHOR_NAME "${VISE_SERVER_AUTHOR_NAME}"
#define VISE_SERVER_AUTHOR_EMAIL "${VISE_SERVER_AUTHOR_EMAIL}"
#define VISE_SERVER_FIRST_RELEASE_DATE "${VISE_SERVER_FIRST_RELEASE_DATE}"
#define VISE_SERVER_CURRENT_RELEASE_DATE "${VISE_SERVER_CURRENT_RELEASE_DATE}"
#endif
